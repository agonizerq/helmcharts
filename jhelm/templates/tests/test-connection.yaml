apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jhelm.fullname" . }}-test-connection"
  labels:
    {{- include "jhelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jhelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
